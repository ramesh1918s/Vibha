apiVersion: v1
kind: Service
metadata:
  name: audit-service
spec:
  selector:
    app: audit
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30001   # optional (range: 30000–32767)
